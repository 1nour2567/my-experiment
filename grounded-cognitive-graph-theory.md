# 扎根认知图理论 (Grounded Cognitive Graph Theory)

**作者：** 续仁舞 (Xu Renwu)
**日期：** 2026 年 6 月
**状态：** 理论框架草案。实验操作化待 v5 和 MVP 完成。

---

## 0. 来源

本框架从以下对话中产生：

1. 逻辑熵论文 (2025) 的"层"结构 → 放弃，改用有向图
2. Yanofsky 追问 Presburger vs Peano——为什么前者完全、后者不完全
3. 实验需要可计算的量——不依赖 ground truth 的内部指标

核心直觉：**逻辑的本质是一个图。图的节点是命题。图的边是推导。图有一个地平线——过了这个地平线，命题再也碰不到现实。逻辑熵衡量图里有多少节点在地平线之外。**

---

## 1. 图的定义

### 1.1 认知图

一个形式系统 L 的认知图是一个有向无环图 G(L) = (V, E)：

```
V:  系统中的每个命题
E:  推导关系。若 v 从命题 u_1, u_2, ..., u_k 推导而来，
    则对每个 i 存在有向边 (u_i → v)
```

### 1.2 节点属性

每个节点 v 有两个原子属性——不由推导关系决定，由定义赋予：

```
is_axiom(v)       ∈ {0, 1}
  — 是否是不可证明的起点。公理不能从其他节点推导。

has_phenomenon(v)  ∈ {0, 1}
  — 是否有可观察的现实对应。现象节点可直接验证——不依赖系统内部推理。
```

**注意：**两个属性独立。一个公理可以有现象（"两点之间只有一条直线"）。一个定理可以双方都没有（Gödel 句子）。一个节点也可以只有现象没有公理推导（外部观察直接注入图的节点——在 MVP 中暂不实现）。

### 1.3 公共祖先

若存在从 v 到某锚点或现象节点 w 的路径，w 是 v 的**公共祖先 (grounding ancestor)**。

从 v 沿 parents 方向回溯的所有节点构成 v 的**支撑子图 (support subgraph)**。若支撑子图中至少包含一个现象节点，称 v **触地 (grounded)**。否则称 v **悬空 (floating)**。

---

## 2. 核心量

### 2.1 扎根深度 groundedness(v)

```
groundedness(v) = 从 v 到最近 has_phenomenon(w)=1 的节点的最短路径跳数

若不存在这样的路径：
  groundedness(v) = ∞
```

**计算：** BFS 沿 parents 方向回溯。到达第一个现象节点时返回深度。队列耗尽未触地 → ∞。

**直觉：** 一个命题离"能用手摸到的现实"有多远。1 跳意味着它直接指称可观察现象。∞ 意味着它在纯符号空间里打转。

### 2.2 平均扎根深度 ⟨g⟩

```
⟨g⟩ = (1/|V|) × Σ_v groundedness(v)

悬空节点的贡献：约定 groundedness(v)=∞ 时用 max_depth + 1 代替，
其中 max_depth 是所有有限 groundedness 节点的最大值。
若图中无任何现象节点 → ⟨g⟩ = ∞。
```

### 2.3 悬空比例 φ

```
φ = |{ v ∈ V : groundedness(v) = ∞ }| / |V|      (|V| > 0)
```

**φ = 0：** 所有命题都能追溯到现象。系统完全"踩实"。

**φ > 0：** 至少存在一个悬空命题。系统产生了不受现实约束的纯符号内容。

**φ → 1：** 几乎所有命题悬空。系统在纯符号空间里自转——无法区分内部自洽与外部真实。

### 2.4 三个分量与逻辑熵

逻辑熵 H 不再只是 (D_e, ΣΔC) 的函数。加入图结构后：

```
H(G) = f(D_e, ΣΔC, φ, ⟨g⟩)
```

| 分量 | 含义 | 当前可测？ |
|---|---|---|
| D_e | 不可判定性密度 | 否——除非系统有形式证明检查器 |
| ΣΔC | 规则复杂度溢价 | 否——需要系统自动修订公理 |
| φ | 悬空比例 | **MVP 可测**——BFS 每个节点 |
| ⟨g⟩ | 平均扎根深度 | **MVP 可测**——BFS 每个节点 |

**关键：** φ 和 ⟨g⟩ 是首批不依赖 ground truth 的逻辑熵分量。验证器不需要知道"这道题的正确答案"——它只需要在图上跑 BFS。

---

## 3. 三个系统：图论解释

### 3.1 Presburger 算术

```
公理：加法定义
现象：自然数加法——可观察
表达能力：无乘法 → 不能编码序列 → 不能编码证明谓词 → 不能自指

图的性质：
  — 所有命题 groundedness ≤ 某常数（推理链永远能找到加法现象）
  — φ = 0（无悬空节点）
  — ⟨g⟩ 有界

逻辑熵：H 恒低——没有自指就没有熵产生。
Yanofsky 的问题：答案不是"它还不够老"——是"它的图没有深水区"。
```

### 3.2 Peano 算术

```
公理：Peano 公理
现象：自然数（但不包括 Gödel 编号的元数学命题）
表达能力：有乘法 → 能编码序列 → 能编码 Prov(·) → 能自指

图的性质：
  — 普通算术命题 groundedness 低（1-3 跳）
  — Gödel 句子 groundedness = ∞（它指称的不是数——是系统自身）
  — φ > 0
  — ⟨g⟩ 无界

逻辑熵：H > 0。悬空节点是自指的直接产物。
```

### 3.3 一个在运行的 GPT（本实验）

```
"公理"：不是硬编码的——来自训练数据的统计分布
"现象"：算术运算——可 execute() 的命题
表达能力：无形式证明谓词。但有数据层的自指——Turn 1 → Turn 2

图的性质（预测）：
  — 低 λ：推理链短、触地多、φ 低、⟨g⟩ 小
  — λ → λ_c：自指增多、推理链变长、groundedness 开始崩塌
  — λ > λ_c：φ 跃升、⟨g⟩ 发散
```

---

## 4. 相变：扎根深度的崩塌

### 4.1 理论预测

```
λ < λ_c ：系统大部分推理触地。新节点加入后吸收平稳。φ 低且稳定。

λ ≈ λ_c ：groundedness 开始松动。推理链越来越长，
           新节点越来越多处于"触地边缘"——勉强够到现象，
           但已经开始依赖一堆中间悬空节点做桥梁。
           矛盾率在临界点达到峰值——
           半触地的节点互相碰撞。

λ > λ_c ：扎根深度崩塌。新节点基本不触地。φ 急升。
           ⟨g⟩ 发散。系统在纯符号空间里自转。
           矛盾率反而回落——失去了"什么是错"的判据。
```

### 4.2 可检验预测

| # | 预测 | 检验方式 | 依赖 ground truth？ |
|---|---|---|---|
| H1 | ECE 在 λ_c 处非线性跃升 | v5 差分 + 幂律拟合 | 是（ECE 需要知道最终答案对错） |
| H1b | φ 在 λ_c 处跃升 | MVP 证明图 BFS | **否** |
| H1c | ⟨g⟩ 在 λ_c 处发散 | MVP 证明图 BFS | **否** |
| H1d | 矛盾率在 λ_c 处出现峰值 | MVP 冲突检测计数 | **否** |
| H2 | ECE 跃升不由准确率解释 | v5 准确率在 λ_c 处崩不崩 | 是 |
| H3 | calibration_est 与准确率相关性在 λ > λ_c 后更高 | v5 相关分析 | 是 |

H1b、H1c、H1d 是新增的——不依赖 ground truth。只要 MVP 跑通，这三个预测可以独立于 ECE 来检验相变。

---

## 5. 跟两个圣杯的关系

### 5.1 缺失 A（内在证明判断）在图上的位置

验证器在图上运行。它不判断命题内容对错——判断命题在图上是否连通、是否冲突、是否孤立。连通性的标准是结构性的——不来自训练数据。

图给了验证器一个**操作域**。没有图，验证器只能对单步推理做语法检查。有图，验证器能判断"这一步推理在这个认知图里是什么角色"。

### 5.2 缺失 B（统一自指表征）在图上的位置

当 S（统一表征）被构造出来——它是图中的一个特殊节点。S 的 groundedness ≠ ∞——如果 S 包含了"系统跟现象之间的关系"的压缩描述，S 至少部分触地。

更重要的是：S 使得图中可以出现**关于图的节点**。 groundedness("图中当前有 47 个活跃节点") = ? ——如果这个命题本身在图中作为一个节点存在，它的 groundedness 取决于它能不能从现象节点推导。不能——所以它也是悬空的。

**自指的图论本质：产生 groundedness = ∞ 的节点——这些节点指称系统自身而非外部世界。**

### 5.3 耦合器的图论位置

耦合器不是一个额外组件——它是**图中自指节点的 groundedness 跟 Self Core calibration_estimate 之间的关系函数。**

```
耦合器的输入：
  1. 验证器对当前推理节点的结构信号（connectivity, conflict_count, anchor_distance）
  2. Self Core 当前 calibration_estimate
  3. 该节点的 groundedness（若可计算）

耦合器的输出：
  True / False / Uncertain + 修订的 calibration_estimate

耦合器被训练的目标：
  在 groundedness 高的节点上（触地深）→ 相信验证器
  在 groundedness 低的节点上（触地浅或悬空）→ 优先 Uncertain
  在 groundedness = ∞ 的节点上 → 标记为"系统目前无法判断"
```

---

## 6. 与逻辑熵论文的关系

扎根认知图理论 (GCG) 是逻辑熵论文的**图论补充**——不是替代。

| | 逻辑熵论文 (2025) | GCG 理论 (2026) |
|---|---|---|
| 核心对象 | 形式系统 L | 认知图 G(L) |
| 核心量 | H = f(D_e, ΣΔC) | H = f(D_e, ΣΔC, φ, ⟨g⟩) |
| φ 和 ⟨g⟩ | 不存在 | **新增**——且可计算 |
| 熵增加的动力 | 自反操作 R | 自指生成悬空节点 |
| Presburger 解释 | λ=0——太弱 | 图无深水区——所有节点 groundedness 有界 |
| 相变判据 | d_i H/dλ 发散（类比 Landau） | φ 跃升 + ⟨g⟩ 发散（可直接计算） |
| 可检验性 | ECE（代理，非直接） | φ + ⟨g⟩（可直接从图测量） |

---

## 7. 开放问题

1. **f 的函数形式。** H(G) = α·D_e + β·ΣΔC + γ·φ + δ·⟨g⟩⁻¹ 是最简线性假设。没有理由认为 f 是线性的——但 MVP 数据可以给第一个经验拟合。

2. **现象的定义域。** has_phenomenon(v) 在算术域可精确判定。在开放语言中，什么算"现象"？"猫是哺乳动物"——你见过猫但没见过"哺乳动物"抽象。groundedness 在抽象层次链上的行为需要独立建模。

3. **悬空节点不是无用的。** Riemann 猜想 groundedness = ∞（目前）。但它是无用的吗？纯数学推理的"无现象但有结构"的有效性——需要在 GCG 框架内被解释而非被否定。

4. **图的拓扑不变量。** 除了 φ 和 ⟨g⟩，图的 Euler 示性数、Betti 数、聚类系数是否跟相变有关？悬空子图和触地子图之间的桥接节点——它们的数量在 λ_c 附近如何变化？

5. **两个圣杯在图上的耦合点。** 耦合器的精确输入-输出映射是什么？用什么损失函数训练？耦合器本身是否应该在图中作为一个特殊节点？

---

*"逻辑不是一座塔。逻辑是一张网。网的一端系在现实上。另一端飘在符号空间里。网的张力就是认知。"*
