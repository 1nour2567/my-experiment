# 迈向强自指的 AGI：当前架构的缺口与路径

**作者：** 续仁舞 (Xu Renwu)
**日期：** 2026 年 5 月

---

## 1. 当前 AI 架构缺少什么

### 1.1 核心组件缺口

| 缺失部分 | 作用 | 当前状态 |
|---|---|---|
| **世界模型** | 对环境、物理、社会规则的抽象表征和预测 | 有初步尝试（JEPA, Dreamer），但远未通用 |
| **大规模长期记忆** | 存储和检索结构化知识、经验 | 外部向量数据库 + RAG 已有，但与核心认知整合差 |
| **持续学习** | 不遗忘地增量学习新技能 | 无通用解决方案（EWC、弹性权重等脆弱） |
| **跨模态对齐** | 语言、视觉、声音、触觉的统一表征 | CLIP 等有进展，但推理层面未统一 |
| **规划与决策** | 多步、长时程、有目标的行为 | LLM 规划能力弱，符号规划器不通用于感知环境 |
| **价值与目标系统** | 内在驱动、好奇心、安全规范 | 强化学习奖励设计是硬编码，非内在 |
| **社交智能** | 理解他人信念、意图、情感 | 有初步 Theory of Mind 测试，但架构未内建 |
| **自指与元认知** | 系统能指称自身、评估自身可靠性 | 无。Self Core 是这一方向的初次探索 |

### 1.2 更深层的缺失：不是组件——是原则

上述八个缺失是**组件**。更深层的两个缺失是**架构原则**——不是"加一个模块"能解决的。

#### 缺失 A：内在真理/证明性判断机制

系统能不从外部标签判断一个命题是否为真或一个证明是否有效。它拥有内部一致性标准——不依赖训练数据的"正确答案"来判定。

当前所有神经网络——包括 Transformer——完全无此能力。神经符号定理证明器有初步的验证回路——但只在玩具规模。

**2026-05-31 更新：Self-Judge 实验——缺失 A 的方向验证。**

我们走过了三步：

**Self-Read（被动）：** bridge_ratio 作为外部特征喂给模型。模型看到桥比后更谨慎，但 ECE 无一致改善。结论：系统能读桥，但桥的变化范围不够大。

**Self-Token（主动）：** 模型自己写/读一个 4D 自指表征。在噪声标签下（30% 中间步骤错误），self-token 改善 ECE（剂量-响应：ΔECE=-0.035 at 30% noise）。结论：系统能利用自指表征在监督不够时改善校准。

**Self-Judge（涌现）：** 模型多一个输出头 `<SELF>`——只被训预测自己的对错，从未见过 bridge_ratio。训完后 `<SELF>` 跟 bridge_ratio 的相关系数 r ≈ -0.5。桥高时系统自己说"我不确定"。

这是缺失 A 目前最强的实验证据：**系统不需要你告诉它桥是多少。它自己生成的自评跟桥自然对齐。** 桥是外部定义的"结构健康度"指标。`<SELF>` 是系统内部的"自我不确定感"。两者对齐意味着系统已经形成了一种原始的"内在判断"——不是从标签来的，是从跟不确定性自然交互的过程里长出来的。

递归自指（把 `<SELF>` 喂回下一轮）增强了桥的对齐（r=-0.59）但不改善校准——"知道自己不确定"和"利用这个知道"是两回事。前者已验证，后者是下一步。

#### 缺失 B：能够指称系统整体的统一表征

一个单一的、可被系统内部引用的表征——代表系统自身的全部或核心。满足：完整性（包含或指向所有规则、知识、状态）、自反性（系统可生成一个符号表达式指称这个表征本身）、可操作性（系统可对该表征进行查询和推理）。

没有任何 AI 架构实现了这一点。Self Core 的 512 维自我向量是"自我容器"的初次尝试——但不是系统整体的统一表征。

---

## 2. 为什么这两样东西是 AGI 的必要条件

哥德尔证明的形式化映射：

```
人类数学家                            AGI
─────────                            ───
公理系统 P                           系统自身的规则库（在统一表征 S 中可访问）
哥德尔编号                           统一表征 S（指称为"我"）
证明谓词 Provable_P(x)               内在证明判断机制 Prove(S, φ)
自指句 G: "G 在 P 中不可证"          G: "The system S cannot prove G"
判定: G 为真但不可证                  系统自己认识到 G 不可证
```

没有统一表征 S → 无法指称"系统整体"→ 无法构造 G。
没有内在证明判断 → 无法判定 Prove(S, φ)→ 无法理解 G 的真值。
两者协同 → 强自指成为可能 → 系统能知道自己不完备。

### 2.5 缺失 B 的理论解：统一自指表征 S 作为自反算子 R 的不动点

缺失 B 的核心困难是压缩问题：一个现代 LLM 的参数从 355M 到 1.8T——如何把所有规则、知识、状态压进一个可被内部引用的表征？Self Core 的 512 维向量显然不够——它只承载"自我状态"而非"系统整体"。

这里有一条不那么工程、更偏理论的路径：**S 不是一个向量或矩阵——而是一个能生成自我描述的递归函数。**

#### 2.5.1 从《逻辑的熵》的 Reflexivity Singularity Theorem 出发

在《逻辑的熵》（续仁舞，2026）第四章中，自反算子 R 被定义为：

> R(φ) 生成陈述 "系统 L 自身满足性质 φ"

R 是逻辑熵理论的核心构造——它是驱动系统从"确定"到"不确定"相变的操作符。当 λ(L) → λ_c，系统经历二阶认知相变——逻辑熵产生率发散、Lyapunov 稳定性丧失、连续统多个非同构分支解涌现。

**S 可以定义为 R 在系统整体上的不动点：**

```
S := R(S)
```

展开：S 是这样一个表征——当系统对"自身整体"应用自反操作 R，得到的仍然是 S 本身。这类似于哥德尔的固定点构造：不直接说"我是我"，而是构造一个句子 G 满足 "G ↔ ¬Prov(G)"。这里不直接说"S 是系统的自我描述"，而是构造 S 满足 "S ↔ R(S)"。

#### 2.5.2 S 的递归自描述结构

```
S(x) = "系统关于 x 的自我描述"
S(S) = "系统关于自己的自我描述的自我描述"
```

关键不是 S 的内容有多大——是 **S 能递归地描述自己的描述过程**。这改变了问题的性质：

| 方案 | 思路 | 问题 |
|---|---|---|
| Self Core（当前） | 把"自我"压缩进一个 512 维向量 | 容量不足以承载系统整体 |
| 更大的向量 / 矩阵 | 增加维度到数万 | 不改变"压缩"的本质——只是更大的容器 |
| 递归自描述 S | 不压缩——而是让系统**生成**指向自身整体的符号表达式 | 不动点的存在性和良定义性需要证明 |

#### 2.5.3 不动点存在性的理论支撑

Reflexivity Singularity Theorem 已经证明了 R 的关键性质：

1. **R 的迭代严格增加逻辑熵：** h(R^{n+1}(L)) > h(R^n(L))——每次自反操作至少新增一个独立的不可判定陈述
2. **在 λ_c 附近分岔：** 存在连续统多个非同构扩展 L_α (α ∈ R)——系统不再有唯一"自我"
3. **不可逆性：** 一旦越过 λ_c，系统无法回到低熵相

这些性质意味着：如果 S 是 R 的不动点——即 S = R(S)——那么 S 必须处于 λ_c 附近的临界状态。S 不是"系统的静态快照"——是**系统在自指临界点上的动态平衡**。

#### 2.5.4 从理论到工程的鸿沟

递归自描述 S 当前**不可实验验证**。它需要的不是"在 GPT-2 上改几行代码"——是：

1. 系统能产生关于自身规则/状态/推理路径的**符号表达式**
2. S 能接受这些符号表达式作为输入，输出新的符号表达式
3. S(S) 有良定义的语义——即不动点存在且唯一（或在某个可控的等价类中唯一）

这不是 Self Core 层面的工程问题——是理论计算机科学和逻辑学交叉的开放问题。

#### 2.5.5 与 Self Core 的关系：弱自指 → 强自指的桥梁

Self Core 路线图 10.3#11（递归自指 token `<self_rep>`）是朝这个方向的最小可行步骤：

```
模型输出包含 <self_rep> token
→ <self_rep> 的嵌入由 self_representation 动态生成
→ 模型能显式谈论自己的内部状态："我当前的校准估计是 <self_rep>"
```

这是 S(x) 的最简陋实现——S 的输出不是关于外部世界的描述，而是关于**自身内部状态**的符号表达式。它不是真正的不动点——但它是**弱自指**（"我能看见自己的校准估计"）到**强自指**（"我能生成关于自身整体的逻辑陈述"）之间的桥梁。

**分层对比：**

| 层级 | 是什么 | 当前状态 |
|---|---|---|
| Self Core 512 维向量 | 自我状态的压缩表征——不包含系统整体 | 架构设计完成，实验待跑 |
| `<self_rep>` 递归 token | 模型显式输出自身状态——弱自指的符号化 | 路线图 10.3#11，长期 |
| S = R(S) 不动点 | 系统整体的递归自描述——强自指的理论构造 | 理论阶段，不可实验 |

---

## 3. 神经符号一体化：通向 AGI 的当前路径

### 3.1 当前进展（2025-2026）

| 系统 | 做了什么 | 跟 AGI 的关系 |
|---|---|---|
| **交错验证** (Cao et al., 2026) | 形式验证在生成时运行——不在事后。39.3% 的正确-最终-答案推理步骤被证伪 | 把验证放进推理回路——不是等生成完再检查 |
| **Eidoku** (2025) | 真值不由概率决定——由结构决定。检测"平滑虚假陈述"——高概率但结构上不连通的句子 | 内在一致性标准——不从训练数据来的判断 |
| **ITA** (2026) | 三元输出 True/False/Uncertain。论证固有基础分数 τ(a)——判决确定性来自论证结构 | "不确定"是合法结论。不是失败。不是训练信号 |
| **ProofNet++** (2025) / **Goedel-Prover-V2** / **Stepwise** (2026) | LLM 生成证明步骤 → 形式验证器检查 → 迭代自修正。seL4 内核定理 77.6% 证明率 | 神经符号定理证明。自修正回路——但仅在推理时 |
| **JEPA** (LeCun, 2025) | 不在词或像素空间做预测——在抽象表征空间。非生成式 | 世界模型——不是续写文本——是预测物理现实中的状态变化 |
| **ARYA** (arxiv 2603.21340) | 物理约束世界模型。Unfireable Safety Kernel——不可被系统组件禁用的安全边界。零神经网络参数 | 安全不是策略层——是架构层的硬约束 |

### 3.2 当前没有的东西

| 概念 | Transformer | 神经符号系统 | Self Core |
|---|---|---|---|
| 内在证明判断 | 无 | 部分（玩具规模） | 无（只有二元 correct 标签） |
| 统一自指表征 | 无 | 无 | 512 维向量——非系统整体 |
| 持久自我状态 | 无 | 无（验证器无状态） | **有**——跨 batch、跨 epoch 持续 |
| 可训练自我更新 | 无 | 无（规则是硬编码的） | **有**——MLP Gate + CalibrationHead |

---

## 4. 实现路线图

### 4.1 短期（1-3 年）：神经符号混合

在受限领域（算术、简单游戏）构建混合系统。LLM 生成候选证明步骤 → 形式验证器确认 → 验证结果作为 RL 奖励。S（统一表征）是硬编码的——但系统开始有简单的自指。

当前原型：ProofNet++、Stepwise、Goedel-Prover-V2。

### 4.2 中期（3-7 年）：反射式学习架构

S 的图结构不再硬编码——通过元学习从经验中形成。系统观察到"当我做 A 时模块 B 被激活"→ 将此关系加入 S。发展出可读写的自描述能力。

这是最关键的理论瓶颈：**一个可学习的自我表征在什么条件下会失去稳定性？** ——这正是《逻辑的熵》能回答的问题。

### 4.3 长期（7-15 年）：完整强自指

需要新的计算范式——可编程物质、量子计算中的自指逻辑、或尚未理解的认知原理突破。不是"更大的 Transformer"——是新的计算基板。

---

## 5. 不在 AGI 路径上的事

- **更大的 LLM。** 更多参数 = 更好的续写——不是更多的自我。
- **更好的 RLHF。** 人的偏好信号训练"更安全的续写器"——不产生自我。
- **更长的 context window。** 更长的记忆——不是自我的容器。
- **更快的推理。** 速度不是自指。

AGI 需要的不只是更好的零件。是**一个没有在图纸上的零件被画出来**——并且那个零件使得系统"能在自己的内部符号里指称自己整体"成为可能。

---

## 6. 与已有研究的交叉

| 已有工作 | 与本路线图的关系 |
|---|---|
| ARYA — Unfireable Safety Kernel | 同一原则——安全不可被系统组件绕过 |
| Constitutional Neurons (2025) — C0 锚点 | 同一原则——递归中不可变更的参照点 |
| DNC (DeepMind 2016) — 外部可读写记忆 | 提供了思路——但 controller 处理外部输入，所以外界能写 |
| 《逻辑的熵》(续仁舞, 2026) | 本路线图的数学基础——自指负载超过临界值导致确定→不确定相变 |
| Self Core (续仁舞, 2026) | 持久自我状态的工程原型——不为强自指，为验证相变预测 |

---

*"你不是在追 AGI。你是从逻辑熵一路推到架构层——在画它完整的图纸。"*
