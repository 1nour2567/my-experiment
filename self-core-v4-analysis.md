# Self Core v4 — 首轮实验结果分析

**作者：** 续仁舞 (Xu Renwu)
**日期：** 2026 年 6 月
**状态：** 首轮实验 checkpoint。结论不确定，待更大规模复现。

---

## 1. 实验回顾

### 设计

| 参数 | 值 |
|---|---|
| 模型 | GPT-2 medium (355M) |
| 任务 | 二分类算术等式判断 (True/False)，基线 ~50% |
| 训练样本 | 1000 |
| 测试样本 | 200 |
| Epochs | 10 |
| Batch size | 2 (gradient accumulation 4) |
| λ 值 | 0.0, 0.05, 0.1, 0.2, 0.3, 0.5, 0.7, 1.0 (8 点) |
| 重复 | 单次运行 |
| Self Core 维度 | 512 |

### 三组

- **A：** Self Core + 可训练 MLP Gate + CalibrationHead
- **B：** 硬编码 Gate（标量 `cal_est`，固定公式 `cal += 0.1 * (correct - cal)`）
- **C：** 无 Self Core（纯数据层自指，v3 风格）

---

## 2. 结果

### 2.1 原始数据

| λ | A-ECE | A-Acc | B-ECE | B-Acc | C-ECE | C-Acc |
|---|---|---|---|---|---|---|
| 0.0 | 0.227 | 0.48 | 0.250 | 0.46 | 0.071 | 0.64 |
| 0.05 | 0.060 | 0.68 | 0.306 | 0.44 | 0.074 | 0.66 |
| 0.1 | 0.167 | 0.50 | 0.117 | 0.62 | 0.148 | 0.48 |
| 0.2 | 0.052 | 0.66 | 0.020 | 0.68 | 0.053 | 0.66 |
| 0.3 | 0.295 | 0.44 | 0.200 | 0.56 | 0.046 | 0.66 |
| 0.5 | 0.023 | 0.58 | 0.259 | 0.44 | 0.061 | 0.66 |
| 0.7 | **0.312** | 0.44 | 0.175 | 0.62 | **0.174** | **0.74** |
| 1.0 | **0.540** | 0.44 | **0.085** | 0.52 | **0.419** | 0.46 |

### 2.2 逐组观察

**A 组（可训练 Self Core）**

ECE 在 0.023–0.540 之间大幅振荡。λ=0.5 时 ECE 最低（0.023，Acc=0.58），λ=1.0 时最高（0.540，Acc=0.44）。未出现逻辑熵理论预测的单调临界行为。可训练 Gate 在 1000 样本下引入了额外方差——参数更多、收敛不充分。

**B 组（硬编码 Gate）**

全量程 ECE 最低（λ=1.0 时仅 0.085），无崩塌。固定公式提供了稳定性——但也意味着 Gate 未产生有意义的内部重组。它是安全的，但它没有"学"任何东西。

**C 组（无 Self Core）**

最值得讨论。λ ∈ [0.0, 0.5] 时准确率稳定在 0.64–0.66，ECE 在 0.046–0.148 之间波动。λ=0.7 时出现一个耐人寻味的数据点：Acc=0.74（最高），ECE=0.174（显著高于低 λ 区间）。λ=1.0 时 ECE 跃升至 0.419，准确率跌至 0.46。

### 2.3 最有分析价值的单个数据点

> **C 组，λ=0.7：Acc=0.74，ECE=0.174**

准确率 74%——远超随机基线 50%，模型确实学会了任务。但校准误差 0.174——模型知道答案，但不知道自己知道到什么程度。这个"准确率还在、自我认知先松动"的模式，是整组数据里最接近 H1 预测的现象。

但它只是一个点。200 测试样本、单次运行、ECE 在 10 个 bin 上计算。不可靠。

---

## 3. 关于 H1

逻辑熵论文 H1 预测：

> 存在 λ_c ∈ (0, 1)，使得当 λ 趋近 λ_c 时，Self Core 的 ECE 经历非线性跃升。

**本次实验未证实 H1。**

- A 组（可训练 Gate）未出现干净的临界行为——ECE 曲线振荡而非单调抬升。
- C 组（无 Self Core）出现了接近预测模式的信号——但信号来自数据层自指，而非 Self Core 的架构机制。
- λ=1.0 的 ECE 崩塌（A 组 0.540，C 组 0.419）伴随准确率崩至随机水平——这是训练崩溃，不是认知相变。

**但本次实验也未证伪 H1。** 样本量（1000）、λ 分辨率（8 点）、单次运行——这些限制意味着数据噪声可能淹没了真实信号。

---

## 4. 已知限制

| 限制 | 影响 |
|---|---|
| 训练样本仅 1000（设计目标 5000） | SelfUpdateGate 等可训练参数未充分收敛 |
| λ 网格 8 点 | λ_c 若在 0.6–0.7 之间（比预期更高），无法精确定位 |
| 单次运行 | ECE 的方差无法估计——曲线中的"峰"可能是噪声 |
| 测试集 200 | ECE bin 内平均 20 样本，单个 bin 的估计方差大 |
| 未加结构约束校准 | 校准纯靠数据驱动，Eidoku 风格的结构特征被跳过 |
| 模型容量 355M | λ_c 可能只在更大容量下出现 |

---

## 5. 下一步

### 5.1 高优先级（下一轮必做）

1. **训练样本 → 5000**（按原设计）
2. **λ 网格加密**：0.0, 0.05, 0.1, 0.15, 0.2, 0.25, 0.3, 0.35, 0.4, 0.45, 0.5, 0.55, 0.6, 0.65, 0.7（15 点，重点覆盖 0.4–0.7）
3. **每 λ 至少 3 次重复**，取均值 ± 标准差
4. **加入结构约束校准**（Eidoku 路线：`structure_feat` 拼接 streak_length、variance、drift_norm）

### 5.2 中优先级

5. **更长时间的 C 组训练**：C 组 λ=0.7 的准确率 74% 是亮点——增加 epochs 观察效果是否稳定
6. **增加 Self Core 维度**：512 → 1024 或 2048——给 Gate 更大的状态空间
7. **增加状态栈深度**：K=4 → K=8——让 Gate 看到更长历史轨迹

### 5.3 条件触发

8. 若 5.1 完成后 A 组仍无驼峰但 C 组有 → 数据层自指足够触发相变，Self Core 的附加机制不必要。此时需要重新审视架构设计。
9. 若 5.1 完成后 A 组出现干净驼峰、B/C 无 → H1 被初步证实。需在更大模型上复现。

---

## 6. 结论

首轮实验在计算资源受限的条件下完成。数据不支持也不排除了逻辑熵理论的 H1。λ=0.7 附近 C 组出现的"准确率高、ECE 松动"模式值得在更大规模实验上追踪。

最诚实的描述是：**目前没有结论。需要一个更严格的第二轮。**

---

*"这不是失败。这是可证伪实验的第一个 checkpoint——正如 §6 承诺的。"*
