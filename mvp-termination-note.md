# MVP 实验终止说明

**日期：** 2026 年 5 月 27 日
**实验：** `mvp-experiment-v1.py` — GPT-2 large + 两步推理 + 结构验证器
**终止于：** A 组 Repeat 1, 6/12 λ 完成

---

## 终止原因

**模型未学会任务。** 所有 λ 下准确率 42-47%——不高于随机基线 50%。

```
λ=0.0  Acc=0.447
λ=0.1  Acc=0.457
λ=0.2  Acc=0.450
λ=0.3  Acc=0.447
λ=0.4  Acc=0.423
λ=0.45 Acc=0.473
```

ECE 同样不可读——模型不会答题，校准误差反映的是随机输出的置信度分布，而非"验证器信号能否替代 ground truth"的任何信息。

## 根本原因

两步推理任务（"计算 a op b → 结果 > c？"）对当前架构——GPT-2 + Self Core + 证明图——过于复杂。模型需要在单次生成中同时完成计算、比较、和格式规范输出。GPT-2 作为因果语言模型，在多步推理的结构化输出上没有经过专门训练。

6 次实验迭代中：
- GPT-2 small + fp32 → OOM
- GPT-2 medium + fp16 → NaN loss
- GPT-2 small + fp32 → Acc=45%（容量不足）
- GPT-2 large + fp32 → Acc=45%（任务太难，非容量问题）

容量升级（small→medium→large）未改善准确率——瓶颈是任务设计，不是模型大小。

## 下一步

MVP 的核心假设（"结构验证器信号能否替代 ground truth 训练校准"）**未被本次实验检验。** 要检验它，需要：

1. **回到单步任务。** v5 的算术等式判断（True/False）——GPT-2 medium 在该任务上 Acc 可达 0.59-0.67。只有模型先学会任务，ECE 数据才有意义。
2. **保留证明图 + 验证器。** 在单步任务上构建推理链——模型只需判断逻辑真值，不需要生成结构化输出。证明图存"命题→真值判断"的关系。
3. **重新跑 A vs C 对比。** 在这个简化任务上检验核心假设。

## 本次实验的可用数据

6 个 λ × 1 次重复的 ECE 和 Acc 数据——由于 Acc < 0.5，数据无法用于分析。不纳入最终报告。

---

*"停掉一个无效实验不是在承认失败——是在不让它继续浪费时间。"*
