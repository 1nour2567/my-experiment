# 逻辑熵 — 物理层术语表 (v2.0)

**作者：** 续仁舞 (Xu Renwu)
**日期：** 2026 年 6 月
**状态：** 草案。等待 v5 实验数据填充"实验值"列。

---

## 0. 阅读说明

这份术语表做三件事：

1. **给每个理论符号下一个精确的定义**——它是什么数学实体
2. **给每个符号指定实验中的操作化**——它在 Self Core 实验里怎么测量
3. **标注每个映射的强度**——是证明、假设、还是启发类比

"类型"列：
- **定义**：纯数学构造，不依赖实验
- **定理**：从定义出发，有严格推导
- **假设**：逻辑上 plausible，但未经严格证明——需要实验检验
- **启发**：来自物理类比的推测，用于指导实验设计——不应声称"已证明"

---

## 1. 核心量

| 符号 | 名称 | 类型 | 理论定义 | 实验操作化 | 论文章节 |
|---|---|---|---|---|---|
| λ | 自反负载 | 定义 | λ(L) = K(π_L) + K(⌜R⌝)，其中 K 是 Kolmogorov 复杂度，π_L 是 L 的公理集，R 是自反算子 | 训练集中含自指闭合（Turn 2）的样本比例。λ=0.3 → 30% 样本是 Turn 1→Turn 2 | Ch. 3, Def 4.2.3 |
| λ_c | 临界自反负载 | 假设 | 使得逻辑熵产生率 d_i H/dλ 发散的最小 λ 值。若同构成立，λ_c 是自由能 F_L 的奇点 | ECE-vs-λ 曲线中出现非线性跃升的 λ 位置。需在多个模型容量上验证标度行为 | Ch. 4, Thm 4.2.1 |
| H(L) | 逻辑熵 | 定义 | H(L) = f(D_e, ΣΔC)，其中 D_e 是不可判定性密度，ΣΔC 是规则复杂度溢价。函数 f 的具体形式未指定——这是当前理论的一个缺口 | ECE（预期校准误差）作为代理变量。理由：H↑ → 系统内部不确定性↑ → 校准估计不可靠 → ECE↑。**注意：ECE 不是 H 的直接度量——它是 H 的一个下游效应。H 本身的直接测量仍未解决** | Ch. 2.3 |
| D_e(L) | 不可判定性密度 | 定义 | D_e := limsup_{n→∞} (1/n) log |{φ ∈ Sent_n(L) : L ⊬ φ ∧ L ⊬ ¬φ}|。≥0，由 Gödel 第一不完备定理保证严格 >0（当 L ⊇ Q） | 无法直接测量。λ 作为控制变量间接操纵——高 λ → 更多自指 → 预期 D_e 更高 | Ch. 2.3 |
| ΣΔC | 规则复杂度溢价 | 定义 | 系统为维持一致性而引入新公理/修改推理规则导致的净复杂度增加之和。ΣΔC = Σ_i (C(F_{i+1}) - C(F_i)) | 无法直接测量。在 MVP 中可通过锚点修订频率间接观察 | Ch. 2.3 |
| d_i H | 内部熵产生率 | 定理 → 假设 | d_i H ≥ 0（非全能定理保证严格 >0）。**定理部分：** Gödel 证明存在不可判定的真命题 → D_e > 0 → H > 0。**假设部分：** d_i H/dλ → +∞ 当 λ → λ_c⁻ 。后者是 Landau 类比的产物，不是定理 | 监测 ECE 随 λ 的变化率 ∆ECE/∆λ。在 λ_c 附近预期加速增长 | Ch. 3, Lemma 4.2.1 |

---

## 2. 类比热力学量（全部为"启发"类型）

**重要声明：** 以下映射来自热力学的形式类比。它们在逻辑系统上没有独立的理论推导——仅在"逻辑-热力学同构假设"下才有意义。实验尚未验证这个假设。

| 符号 | 名称 | 类型 | 理论定义 | 实验操作化 | 论文章节 |
|---|---|---|---|---|---|
| U_L | 逻辑内能 | 启发 | U_L = K(L) + H_known。K(L) 是系统 L 的 Kolmogorov 复杂度（"结构能量"），H_known 是系统已证明的定理库所含的信息量（"储存能量"） | 无法直接测量。K(L) 不可计算（Chaitin 不完备定理）。H_known 在 GPT 架构中没有自然的对应 | Ch. 4.3.1 |
| T_L | 逻辑温度 | 启发 | T_L = (∂H/∂U_L)^{-1}。高 T_L → 单位内能增加带来较大熵增 → 系统处于"认知激发态"，证明搜索易发散 | calibration_estimate 可能是一个代理：高 T_L → 系统自知不确定 → cal_est 低。**但 T_L 和 cal_est 的精确映射未建立** | Ch. 4.3.1 |
| W_L | 逻辑功 | 启发 | 证明过程的计算代价。P vs NP 在这个框架下的表述：寻找使 W_L 最小的证明路径 | 可粗略对应每次推理的 FLOPs。但未纳入当前实验 | Ch. 4.3.1 |
| Q_L | 逻辑热 | 启发 | Q_L = ∆U_L - W_L。非机械过程（直觉跳跃、审美选择）吸收的信息 | 无实验对应 | Ch. 4.3.1 |
| F_L | 逻辑自由能 | 启发 | F_L = U_L - T_L · H。平衡态下 F_L 最小。相变定义为 F_L 在 λ_c 处的非解析性 | **当前实验未测量 F_L。** 但 F_L 比 H 更应该是"相变发生在哪里"的判据——相变在 F_L 的奇点上，不在 H 的极值上。这是一个未解决的问题 | 论文中未明确定义，需添加 |

---

## 3. 临界现象（全部为"启发"类型）

| 符号 | 名称 | 类型 | 定义 | 实验检验 |
|---|---|---|---|---|
| β | 序参量临界指数 | 启发 | ⟨R⟩ ~ (λ - λ_c)^β，λ > λ_c。R 是"自反序参量"——系统在多大程度上处于自反状态 | 需定义 R 的操作化。可能候选：自洽接受率、Uncertain 输出比例。β 的值不能从理论推导——需拟合 |
| γ | 响应函数临界指数 | 启发 | χ(λ) ~ \|λ - λ_c\|^{-γ}，其中 χ = ∂⟨R⟩/∂λ | d(ECE)/dλ 在 λ_c 附近的发散行为。需在多点 λ 网格上拟合 |
| ν | 关联长度临界指数 | 启发 | ξ(λ) ~ \|λ - λ_c\|^{-ν} | 无当前实验对应。"逻辑空间中的距离"未定义 |
| z | 动力学临界指数 | 启发 | τ ~ ξ^z，其中 τ 是弛豫时间 | 可对应训练收敛所需的 epoch 数随 λ 的变化。λ→λ_c 时训练是否变慢？ |

**关键假设：** 如果逻辑-热力学同构成立，则 β、γ、ν 应该是普适的——不依赖模型容量、不依赖任务域。Ising 模型和液氦相变共享相同的临界指数——如果 Self Core 在算术域和文本域产生不同的 β，同构假设被削弱。

---

## 4. 实验测量的完整推理链

```
理论层                        实验层
──────                        ──────
λ (自反负载)          ←→     训练数据中自指样本的比例（可控）
                              注意：理论 λ = K(π_L)+K(⌜R⌝)，实验 λ =
                              Turn 2 比例。不是同一个东西——是代理。

H (逻辑熵)            ←?→    ECE（代理，非直接度量）
      │                       推理链（每步都有假设）：
      │                       ① λ↑ → 自指频率↑ → D_e↑（假设：更多自指
      │                          意味着更多不可判定命题。未严格证明）
      │                       ② D_e↑ → H↑（定义上成立，但 f 的形式未知）
      │                       ③ H↑ → 系统内部不确定性↑（假设：熵增加意味
      │                          着系统的内部状态更不确定。同义反复？）
      │                       ④ 内部不确定性↑ → ECE↑（假设：系统不能准确
      │                          估计自己的可靠性。plausible 但未证明）

λ_c (临界负载)        ←→     ECE 非线性跃升的 λ 位置（待观察）
                              注意：这是倒推——从数据找 λ_c，不是从理论
                              预测 λ_c。理论的预测力体现在：如果换了模型
                              容量/任务，λ_c 按标度律移动吗？

T_L (逻辑温度)        ←?→    calibration_estimate（非常间接的代理）
                              映射方向：高 T_L → 系统自知不确定 → cal_est
                              低。但 T_L 本身从未被独立测量过——这是最弱
                              的映射
```

---

## 5. 当前理论的缺口（按严重程度排序）

| # | 缺口 | 严重性 | 可能的修补方向 |
|---|---|---|---|
| 1 | H = f(D_e, ΣΔC) 的 f 未指定 | **高**。没有 f，H 无法从 D_e 和 ΣΔC 计算。论文声称 H 是"量"，但实际上只是一个概念框架 | 在最简单的情况下假设 f 是线性：H = α·D_e + β·ΣΔC。从实验中拟合 α, β。或者在压缩锚点的 MVP 里给一个算法定义 |
| 2 | λ 的理论定义和实验定义不一致 | **高**。理论 λ = K(π_L)+K(⌜R⌝) 不可计算。实验 λ = Turn 2 样本比例。需要论证后者是前者的单调函数 | 用 Chaitin 不完备定理：一个系统能证明的 Ω 前缀长度受限于 K(系统)。更多自指样本 → 系统被迫处理更多"自己生成的内容" → 等效于更高的自反复杂度。写出这个论证 |
| 3 | ECE 作为 H 代理的推理链未经检验 | **高**。四步推理链每步都是 plausible 但不是 proved | v5 数据出来后：如果 ECE 对 λ 敏感但对随机种子不敏感 → 证据在增强。如果 β（临界指数）在不同容量下一致 → 强证据。在这个推理链上加入明确的 caveat |
| 4 | F_L 未被定义但应该是核心量 | **中**。相变发生在 F_L 的奇点上，不是 H 的。H 可以平滑增加但没有相变——相变需要 F_L 的非解析性 | 定义 F_L = U_L - T_L·H。但 U_L 和 T_L 都不能直接测量。在实验里可能需要找 F_L 的代理——但目前没有 |
| 5 | T_L 缺乏独立测量 | **中**。calibration_estimate 是 T_L 的代理，但没有独立的 T_L 测量来校准这个代理 | 寻找一个独立于 cal_est 的 T_L 指标——例如证明图的平均连通度、自指循环的检测频率 |
| 6 | U_L 不可计算 | **中**。K(L) 的不可计算性是 Chaitin 定理的结论——这意味着 U_L 在原则上不可精确测量 | 用压缩算法（如 gzip）对模型权重的压缩比作为 K 的上界近似。这是一个 pragmatic 的替代 |
| 7 | 临界指数的普适性未被检验 | **低**（当前阶段）。需要至少两个不同容量的模型才能开始检验 | v5 之后：在 GPT-2 large 上重复关键 λ 范围。比较 β 是否一致 |

---

## 6. 建议：下一版论文的结构调整

```
Part I:  数学基础（定理）
         — 非全能定理（严格证明，不依赖物理类比）
         — 不完备性作为 D_e > 0 的数学根源

Part II: 同构假设（推测，Conjecture）
         — 逻辑-热力学同构的陈述
         — Landau 类比：如果同构成立，预期以下标度行为
         — λ_c 的存在性（理论假设，等待实验检验）

Part III: 实验检验（实证）
         — 操作化定义：λ 和 ECE 的测量协议
         — 推理链：从 H 到 ECE 的映射（含每条假设的声明）
         — Self Core 架构 + v5 结果

Part IV: 开放问题
         — f 的具体形式
         — T_L 和 U_L 的操作化
         — 普适性的检验路径
```

---

*"理论的成熟不是它回答了多少问题——是它知道自己哪些问题还没回答。"*
